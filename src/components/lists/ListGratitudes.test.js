import React from 'react';
import { shallow } from 'enzyme';
import ListGratitudes from './ListGratitudes';

describe('ListGratitudes', () => {
    it('renders', () => {
        shallow(<ListGratitudes />)
    })
});